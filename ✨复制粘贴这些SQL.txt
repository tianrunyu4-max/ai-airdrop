========================================
V4.2 参数热更新 - SQL执行清单
========================================

重要提示：
1. 打开 https://supabase.com/dashboard
2. 进入 SQL Editor
3. 依次复制粘贴下面的SQL
4. 每条SQL单独执行
5. 看到 UPDATE 1 或 Success 即成功

========================================
SQL #1 - 对碰奖（7U → 10U）
========================================

UPDATE system_params SET param_value = 10 WHERE param_key = 'pairing_bonus_per_pair';

预期结果：UPDATE 1


========================================
SQL #2 - 复投阈值（300U → 200U）
========================================

UPDATE system_params SET param_value = 200 WHERE param_key = 'reinvest_threshold';

预期结果：UPDATE 1


========================================
SQL #3 - 平级奖层级（8代 → 3代）
========================================

UPDATE system_params SET param_value = 3 WHERE param_key = 'level_bonus_depth';

预期结果：UPDATE 1


========================================
SQL #4 - 平级奖金额（确认2U）
========================================

UPDATE system_params SET param_value = 2 WHERE param_key = 'level_bonus_per_person';

预期结果：UPDATE 1


========================================
验证SQL - 检查参数是否正确
========================================

SELECT 
  param_key as "参数名", 
  param_value as "当前值", 
  param_unit as "单位",
  description as "说明"
FROM system_params
WHERE category = 'binary'
ORDER BY param_key;

预期结果：应该看到10行数据
重点检查这4个：
✅ pairing_bonus_per_pair = 10
✅ reinvest_threshold = 200
✅ level_bonus_depth = 3
✅ level_bonus_per_person = 2


========================================
可选SQL - 平台联系方式配置
========================================

INSERT INTO system_config (key, value, description) VALUES
(
  'platform_contacts',
  '{
    "bilibili": "https://space.bilibili.com/你的B站ID",
    "youtube": "https://youtube.com/@你的频道",
    "telegram": "https://t.me/你的群组",
    "wechat": "AI_TECH_2025",
    "shipin": "搜索\"AI科技创新\"",
    "tiktok": "@aitech_official"
  }'::jsonb,
  '平台官方联系方式'
)
ON CONFLICT (key) DO UPDATE
SET value = EXCLUDED.value,
    updated_at = NOW();

预期结果：INSERT 0 1 或 Success


========================================
完成确认
========================================

全部执行成功后，您将拥有：
✅ 对碰奖：10U（提升43%）
✅ 复投阈值：200U（降低33%）
✅ 平级奖：追溯3代（减少5代）
✅ 参数热更新：1分钟内自动生效

下一步：
1. 打开 https://eth10.netlify.app
2. 登录管理后台
3. 进入"系统参数配置"
4. 看到绿色提示："参数热更新已启用（V4.2）"
5. 可以随时修改参数，1分钟后自动生效！

🎉 恭喜完成！


